<%
var slices = page.getSliceZone('page.body').slices;

for(var i = 0; i < slices.length; i++) {
    var slice = slices[i];

    %><%- include(slice.sliceType, { slice: slice }); %><%
}
%>

<script>
    $(document).ready(function () { $('a').each(function () { this.href = this.href.replace(/^http:\/\/l\//, '/<%= lang %>') }) })
</script>
