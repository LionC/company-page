<!DOCTYPE html>
<html lang="<%= lang %>">

<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1, user-scalable=no">

    <%- include(headTemplate); %>

    <!-- TODO Favicon -->

    <link href="https://fonts.googleapis.com/css?family=Lato:400,300,700" rel="stylesheet" type="text/css">

    <link rel="stylesheet" href="/styles/style.css">

    <link rel="icon" type="image/png" href="/favicon.png" />

    <script type="application/ld+json">
        <%- include('company-jsonld'); %>
    </script>
</head>

<body>

    <nav>
        <img src="/assets/images/logo.png" alt="Acomodeo">

        <div class="nav-container">
            <div class="nav-links">
                <%
                for(var i = 0; i < pages.length; i++) {
                    var p = pages[i];

                    if(!p.getBoolean('page.isInNavigation'))
                    continue;
                    %>
                    <a
                    <% if((template == 'blog-post') ?  (p.uid == 'blog') : (page.uid == p.uid)) { %>
                        class="active"
                        <% } %>
                        href="/<%= lang %>/<%= p.uid %>"><%= p.getGroup('page.navigationName').toArray()[0].getText(lang) %>
                        </a>
                        <%
                    }
                    %>
            </div>

            <div class="language-links">
                <a href="<%= languageLinks['en'] %>" <% if(lang == 'en') { %>class="active" <% } %>>en</a> /
                <a href="<%= languageLinks['de'] %>" <% if(lang == 'de') { %>class="active" <% } %>>de</a>
            </div>

            <button onclick="$('.nav-container').removeClass('shown')"><img src="/assets/images/close.png" alt="menu"></button>
        </div>

        <button onclick="$('.nav-container').addClass('shown')"><img src="/assets/images/menu.png" alt="menu"></button>
    </nav>

    <style>
        header::before {
            content: ' ';
            position: fixed;
            width: 100%;
            height: 100%;
            top: 0;
            left: 0;
            background: linear-gradient( rgba(0, 0, 0, .5), rgba(0, 0, 0, .5) ), url("<%= template == 'blog-post' ? blogPost.getImage('blog-post.previewImage').url : page.getImage('page.headerImage').url %>");
            background-size: cover;
            background-repeat: no-repeat;
            background-position: center center;
            will-change: transform;
            z-index: -1;
        }
    </style>

    <header>
        <div class="container">
            <% if(template == 'blog-post') { %>
                <h1>Blog</h1>
            <% } else { %>
                <%- page.getGroup('page.headerText').toArray()[0].getStructuredText(lang).asHtml() %>
            <% } %>
        </div>
    </header>

    <main>
        <%- include(template); %>
    </main>

    <footer>

    </footer>

    <script src="https://code.jquery.com/jquery-3.1.1.min.js" integrity="sha256-hVVnYaiADRTO2PzUGmuLJr8BLUSjGIZsDYGmIJLv2b8=" crossorigin="anonymous"></script>

    <script>
        var nav = $('nav');

        $(window).scroll(function() {
            if ((document.documentElement.scrollTop || document.body.scrollTop) > 100) {
                nav.addClass('scrolled');
            } else {
                nav.removeClass('scrolled');
            }
        });
    </script>

</body>

</html>
