<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1, user-scalable=no">

    <title>Acomodeo</title>

    <!-- TODO Favicon -->

    <!-- <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous"> -->

    <link href="https://fonts.googleapis.com/css?family=Lato:400,300,700" rel="stylesheet" type="text/css">

    <link rel="stylesheet" href="/styles/style.css">
</head>

<body>

    <nav>
        <img src="/assets/images/logo.png">

        <div class="nav-links">
            <a <% if(page == 'aboutus') { %>class="active" <% } %>href="aboutus">About Us</a>
            <a <% if(page == 'products') { %>class="active" <% } %>href="products">Products</a>
            <a <% if(page == 'operators') { %>class="active" <% } %>href="operators">For Operators</a>
            <a <% if(page == 'customers') { %>class="active" <% } %>href="customers">For Customers</a>
            <a <% if(page == 'ask') { %>class="active" <% } %>href="/ask">Ask</a>
        </div>
    </nav>

    <header>
        <div class="container">
            <div class="header-text-container">
                <h1>the world of serviced apartments</h1>
            </div>
        </div>
    </header>

    <main>
        <%- include(page); %>
    </main>

    <footer>

    </footer>

    <script src="https://code.jquery.com/jquery-3.1.1.min.js" integrity="sha256-hVVnYaiADRTO2PzUGmuLJr8BLUSjGIZsDYGmIJLv2b8=" crossorigin="anonymous"></script>

    <script>
        $(window).scroll(function() {
            if ($(document).scrollTop() > 100) {
                $('nav').addClass('scrolled');
            } else {
                $('nav').removeClass('scrolled');
            }
        });
    </script>

</body>

</html>
